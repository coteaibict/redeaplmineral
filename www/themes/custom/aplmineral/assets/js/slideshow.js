(function ($) {
  var $slideshow = $('.view-display-id-slideshow .view-content')
  var options = {
    adaptiveHeight: true,
    autoplay: false,
    autoplaySpeed: 5000,
  }

  $slideshow.slick(options)
})(jQuery)
